{
  "name": "duty-deduction-records",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 5173",
    "build:campuses": "node scripts/build_campus_pages.mjs"
  }
}
